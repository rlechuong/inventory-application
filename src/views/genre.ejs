<%- include("./partials/header") %>

<h1><%= genre.name %> Games</h1>

<% if (games.length === 0) { %>
  <p>No games in this genre.</p>
<% } else { %>
  <ul>
    <% games.forEach(game => { %>
      <li><%= game.title %></li>
    <% }) %>
  </ul>
<% } %>

<%- include("./partials/footer") %>