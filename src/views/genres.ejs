<%- include("./partials/header") %>

<h1>Genres</h1>

<% if (genres.length === 0) { %>
  <p>No genres found.</p>
<% } else { %>
  <ul>
    <% genres.forEach(genre => { %>
      <li><%= genre.name %></li>
    <% }) %>
  </ul>
<% } %>

<%- include("./partials/footer") %>