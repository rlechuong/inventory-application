<%- include("./partials/header") %>

<h1>Games</h1>

<% if (games.length === 0) { %>
  <p>No games found.</p>
<% } else { %>
  <ul>
    <% games.forEach(game => { %>
      <li><%= game.title %></li>
    <% }) %>
  </ul>
<% } %>

<%- include("./partials/footer") %>
